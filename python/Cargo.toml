[package]
name = "ldsc-python"
version = "0.5.0"
edition = "2024"
license = "GPL-3.0-only"
publish = false

[lib]
name = "_native"
crate-type = ["cdylib"]

[dependencies]
ldsc = { path = ".." }
pyo3 = { version = "0.29", features = ["abi3-py39"] }

